import React from "react";
import "./loginError.css";
const loginError = props => (
    <div>
        <h2>What a bummer...</h2>
        <p>Your attempt to login was unsuccessful.</p>
        <hr />
    </div>
);
export default loginError;
